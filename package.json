{
  "name": "nodebox-website",
  "version": "1.0.0",
  "private": true,
  "description": "nodebox.net — built with Eleventy",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
